<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.tiktokcommodity.mapper.ComMapper" >
    <resultMap id="getcommodityRM" type="com.example.tiktokcommodity.bean.Commodity">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="price" property="price"/>
        <result column="price" property="price"/>
        <result column="image" property="image"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_user" property="createUser"/>
        <result column="update_user" property="updateUser"/>
        <association property="category"
                     select="com.example.tiktokcommodity.mapper.ComMapper.getCategoryById"
                     column="category_id"/>
    </resultMap>

    <select id="getCommodityById" resultType="com.example.tiktokcommodity.bean.Commodity" resultMap="getcommodityRM">
        select * from commodity
        where id = #{id}
    </select>

    <select id="getCommodityPreviewVoById" resultType="com.example.tiktokcommodity.bean.CommodityPreviewVo">
        select id,name,price,image from commodity
        where id = #{id}
    </select>

    <select id="getLatestCommodityPreviewVos" resultType="com.example.tiktokcommodity.bean.CommodityPreviewVo">
        select id,name,price,image from commodity
        order by create_time
            limit #{amount}
    </select>

    <select id="getCategoryById" resultType="com.example.tiktokcommodity.bean.Category">
        select * from category where id = #{id}
    </select>

    <insert id="addCommodity">
        INSERT INTO commodity (`name`,category_id,price,image,description,`status`,create_time,create_user)
        VALUES(#{name}, #{categoryId}, #{price}, #{image}, #{description}, #{status}, #{createTime}, #{createUser})
    </insert>

    <update id="updateCommodity">
        UPDATE commodity
        <set>
            <if test="name != null">
                name = #{name}
            </if>>
            <if test="category != null">
                category = #{categoryId}
            </if>>
            <if test="price != null">
                price = #{price}
            </if>>
            <if test="image != null">
                image = #{image}
            </if>>
            <if test="description != null">
                description = #{description}
            </if>>
            <if test="status != null">
                status = #{status}
            </if>>
            <if test="createTime != null">
                create_time = #{createTime}
            </if>>
            <if test="updateTime != null">
                update_time = #{updateTime}
            </if>>
            <if test="createUser != null">
                create_user = #{createUser}
            </if>>
            <if test="updateUser != null">
                update_user = #{updateUser}
            </if>>
        </set>>
        WHERE id = #{id}
    </update>

    <delete id="deleteCommodityById">
        DELETE FROM commodity
        WHERE id = #{id}
    </delete>
</mapper>